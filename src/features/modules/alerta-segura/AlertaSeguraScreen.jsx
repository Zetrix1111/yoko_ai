import ModuleLayout from '../ModuleLayout';

// Módulo: Alerta Segura
// Endpoint backend: POST /api/alerta_segura  (api/alerta_segura.py)
export default function AlertaSeguraScreen({ user, onOpenModules }) {
  return (
    <ModuleLayout title="Alerta segura" onOpenModules={onOpenModules}>
      {/* Construye aquí el formulario. */}
    </ModuleLayout>
  );
}
