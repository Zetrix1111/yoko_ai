import ModuleLayout from '../ModuleLayout';

// Módulo: Seguimiento caja chica
// Endpoint backend: POST /api/caja_chica  (api/caja_chica.py)
export default function CajaChicaScreen({ user, onOpenModules }) {
  return (
    <ModuleLayout title="Seguimiento caja chica" onOpenModules={onOpenModules}>
      {/* Construye aquí el formulario. */}
    </ModuleLayout>
  );
}
